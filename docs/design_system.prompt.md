Create a detailed design system based on the files in `/express'. Analyze the visual design and create a comprehensive style guide including:

1. Typography:
   - Font families
   - Font sizes and weights
   - Line heights
   - Letter spacing

2. Spacing and Layout:
   - Grid system (if applicable)
   - Margin and padding patterns
   - Section spacing
   - Responsive behavior
   - Alignment rules

3. Components:
   - Buttons (styles, states: hover, active, disabled)
   - Form elements / Input fields (styles, states: focus, error, default).
   - Navigation patterns
   - Cards, modals, containers, or other reusable UI elements.

4. Color System:
   - Primary, secondary, accent colors
   - Background colors
   - Text colors
   - Gradient patterns
   - Neutral tones (e.g., grays, whites, blacks).
   - Hex codes for each color.

5. Visual Hierarchy:
   - Heading treatments
   - Emphasis patterns
   - Content organization

6. Animation and Interaction:
   - Hover states
   - Transition styles
   - Scrolling behaviors

7. Icons and Imagery:

   - Icon styles (e.g., line, filled, etc.).
   - Image dimensions and usage guidelines.

Ensure the design system is clear, structured, and easy to follow. Use concise descriptions and include examples where possible