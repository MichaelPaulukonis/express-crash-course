The astro setup is an empty framework - it might work better to host multiple projects.

Express itself is perfectly set up in static, non-modular pages within the `/express/` folder.

Launch it with Live Server.